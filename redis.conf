# Memory Management
maxmemory 512mb
maxmemory-policy volatile-lru

# Persistence (RDB Snapshots)
save 900 1
save 300 10
save 60 10000
dbfilename dump.rdb
dir /data

# Network & Security
protected-mode no
tcp-keepalive 300
